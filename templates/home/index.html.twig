{% extends 'base.html.twig' %}

{% block stylesheets %} 
    <link rel="stylesheet" href="{{ asset('assets/styles/home.css') }}">
{% endblock %}

{% block title %}Accueil{% endblock %}

{% block body %}
    <main class="d-flex flex-column align-items-center justify-content-center flex-grow-1">

        <section class="about py-5">
            <div class="container text-center">
                <h2>À propos de Stubborn</h2>
                <p class="lead">Stubborn est une marque de vêtements basée à Londres qui allie confort et style avec une attitude audacieuse. Notre collection de sweat-shirts haut de gamme est conçue pour les individus qui refusent de faire des compromis sur leur look. Chaque sweat-shirt est fabriqué avec des matériaux de qualité et une attention particulière aux détails, en faisant ainsi non seulement un vêtement, mais une véritable déclaration.</p>
            </div>
        </section>

        <section class="product_list row g-4 ">
            {% for sweatShirt in featuredSweatshirts %}
            <div class="col-4">
                <div class="card w-100">
                    <img src="{{ asset('assets/images/' ~ sweatShirt.img) }}"  class="card-img-top">
                    <div class="card-body d-flex justify-content-between align-items-center">
                        <div>
                            <h5 class="card-title">{{sweatShirt.name}}</h5>
                            <p>{{sweatShirt.price}} €</p>
                        </div>
                        {% if app.user %}
                        <a href="{{ path('app_product', {id: sweatShirt.id}) }}" class="card_btn">VOIR</a>
                        {% endif %}
                    </div>
                </div>
            </div>
            {% endfor %}
        </section>
    </main>  
{% endblock %}